# ============================================================
# FLock Agent API - Environment Configuration
# ============================================================

# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=flock_agent

# Optional: Database URL (alternative to individual settings)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/flock_agent
